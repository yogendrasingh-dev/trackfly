import { RouteShellScreen } from "@/features/foundation/screens/RouteShellScreen";

export default function AccountRoute() {
  return (
    <RouteShellScreen
      title="Account entry"
      description="The simulated authentication route group is prepared. Its product UI begins in Phase 3."
    />
  );
}
