import { RouteShellScreen } from "@/features/foundation/screens/RouteShellScreen";

export default function SupportingShellRoute() {
  return (
    <RouteShellScreen
      title="Supporting flows"
      description="Detail, editor, settings, profile, and Pro destinations will be added only in their tracked phases."
    />
  );
}
